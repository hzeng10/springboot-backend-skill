# .env.sample — 复制为 .env 并填入本地真实值
# .env 已列入 .gitignore，永远不提交真实密钥

# 数据库（本地开发用，生产通过 K8s Secret / 云参数存储注入）
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=

# JWT
JWT_SECRET=
JWT_EXPIRATION_MS=

# 其他应用配置
# APP_SOME_FEATURE_ENABLED=
